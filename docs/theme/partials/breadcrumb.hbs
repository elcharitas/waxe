{{#if parent}}
    {{#with parent}}{{> breadcrumb}}{{/with}}
    <li>
        {{#if url}}
            <a href="{{buildURL url}}">{{name}}</a>
        {{else}}
            <span>{{name}}</span>
        {{/if}}
    </li>
{{else}}
    {{#if url}}
        <li>
            <a href="{{buildURL url}}">{{ name }}</a>
        </li>
    {{/if}}
{{/if}}
